[pytest]
# Collect only the maintained suite. docs/reconciliation-history/ contains an
# archived test_model.py from the superseded Drive review, kept as evidence; it
# targets the pre-reconciliation API and must not run in CI.
testpaths = tests
